name: tv_series
description: TV Series feature module (domain, data, presentation) for ditonton.
publish_to: 'none'
version: 1.0.0

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  flutter:
    sdk: flutter

  core:
    path: ../core

  bloc: ^9.0.0
  cached_network_image: ^3.4.1
  dartz: ^0.10.1
  equatable: ^2.0.5
  flutter_bloc: ^9.1.1
  flutter_rating_bar: ^4.0.1
  http: ^1.2.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  mockito: ^5.0.8
  build_runner: ^2.0.4
  bloc_test: ^10.0.0
  mocktail: ^1.0.5

flutter:
  uses-material-design: true
  assets:
    - assets/
